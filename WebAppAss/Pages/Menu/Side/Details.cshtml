@page "{name}"
@model WebAppAss.Pages.Menu.Side.DetailsModel
@{
    ViewData["Title"] = "Side Details";
}

<div class="container mt-5">
    <div class="row">
        <!-- Left Column: Large Image -->
        <div class="col-md-6">
            @if (Model.Side.ImageData != null)
            {
                <img src="data:image;base64,@Convert.ToBase64String(Model.Side.ImageData)"
                     alt="@Model.Side.ImageDescription"
                     class="img-fluid" >
            }
            else
            {
                <img src="/images/no-image.png" alt="No image available" class="img-fluid" >
            }
        </div>

        <!-- Right Column: Details and Add to Basket -->
        <div class="col-md-6">
            <h2>@Model.Side.Name</h2>
            <p>@Model.Side.Description</p>
            <p>
                <strong>Size:</strong> @Model.Side.Size
            </p>
            <h3>@Model.Side.Price.ToString("C")</h3>

            <!-- Add to Basket Form -->
            <form method="post" asp-page-handler="Buy">
                <input type="hidden" name="Id" value="@Model.Side.Id" >
                <input type="hidden" name="itemType" value="Side" >

                <div class="form-group">
                    <label for="quantity">Quantity:</label>
                    <input type="number" class="form-control" id="quantity" name="quantity" value="1" min="1" max="20" >
                </div>

                <button type="submit" class="btn btn-primary">Add to Basket</button>
            </form>
        </div>
    </div>
</div>
